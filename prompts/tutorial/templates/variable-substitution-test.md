# Variable Substitution Test Examples

This file contains test examples for the variable substitution feature.

## Example 1: File and Line Debug

Fix the bug in `{FILE_PATH}` on line `{LINE_NUMBER}`.

## Example 2: Function Investigation

Debug the function `{FUNCTION_NAME}` in file `{FILE}` and check if it handles the `{INPUT_TYPE}` input correctly.

## Example 3: Multiple Same Variable

Compare `{FILE}` with `{FILE}` and log the differences to `{OUTPUT}`.

## Example 4: Configuration Update

Update the configuration in `{CONFIG_FILE}` to set `{PROPERTY_NAME}` to the value `{PROPERTY_VALUE}`.

## Example 5: No Variables

This prompt has no variables and should work normally without showing the variable modal. It should go directly to the Jules account selection dialog.

## Example 6: Single Variable

Please review `{FILE_PATH}` for code quality issues.
