name: E2E Tests

# Temporarily disabled while fixing smoke tests - uncomment when ready for full E2E testing
# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]
#   schedule:
#     - cron: '0 0 * * *'  # Daily at midnight UTC
#   workflow_dispatch:  # Allow manual triggers

on:
  # Only allow manual triggers for now
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 30
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      issues: write
      pull-requests: write
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        browser: [chromium, firefox, webkit]
        include:
          - os: windows-latest
            browser: chromium
          - os: macos-latest
            browser: webkit
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Install Playwright browsers
      run: npx playwright install --with-deps ${{ matrix.browser }}
    
    - name: Setup Python (for dev server)
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Run E2E tests
      run: npm run test:e2e -- --project=${{ matrix.browser }}
      env:
        BASE_URL: http://localhost:3000
        CI: true
    
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report-${{ matrix.os }}-${{ matrix.browser }}
        path: playwright-report/
        retention-days: 30
    
    - name: Upload test videos
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: test-videos-${{ matrix.os }}-${{ matrix.browser }}
        path: test-results/
        retention-days: 7
    
    - name: Upload test artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-artifacts-${{ matrix.os }}-${{ matrix.browser }}
        path: |
          test-results/
          playwright-report/
        retention-days: 7

  report:
    name: Publish Test Report
    runs-on: ubuntu-latest
    needs: test
    if: always()
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: all-reports
    
    - name: Publish test report
      uses: actions/upload-artifact@v4
      with:
        name: combined-test-report
        path: all-reports/
        retention-days: 30
