check all open and ready to merge tagged PRs greater than {PR-NUMBER}
sort them by importance 1to100 using the diff so I can sequentially merge them
once that is done go by yourself sequentially down this list
and look with which earlier PRs they collide (merge or logical conflict)
output all of this into a markdown file
